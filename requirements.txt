# Interface utilisateur
PySide6>=6.6.0

# Traitement image
Pillow>=10.2.0
opencv-python>=4.9.0
pillow-heif>=0.16.0           # décodage HEIC/HEIF (photos iPhone)
rawpy>=0.19.0                 # décodage RAW (CR2/NEF/ARW/DNG/ORF/RW2), embarqué dans l'exe packagé

# Base de données / indexation
# SQLite est inclus dans Python (stdlib)

# Lecture EXIF / GPS
piexif>=1.1.3

# Reconnaissance faciale
insightface>=0.7.3           # ArcFace R100 + SCRFD-10GF (buffalo_l)
onnxruntime-gpu>=1.17.0      # backend ONNX pour insightface (CUDA 12, RTX 4060)
nvidia-cudnn-cu12>=9.0       # cuDNN 9 via pip (requis par onnxruntime-gpu sur Windows)
scikit-learn>=1.4.0          # PCA + utilitaires ML
hdbscan>=0.8.38              # HDBSCAN C++/Cython, algo Borůvka O(n log²n)

# Hashing perceptuel (détection doublons)
imagehash>=4.3.1

# Carte géographique
folium>=0.16.0

# Export PDF (planches contact, diaporamas)
reportlab>=4.1.0

# Monitoring système (RSS mémoire pour le journal de threads)
psutil>=5.9.0

# Suppression via la corbeille Windows (jamais d'effacement définitif direct)
Send2Trash>=1.8.2

# Packaging
pyinstaller>=6.4.0

# Tests
pytest>=8.0.0
pytest-qt>=4.4.0             # tests de widgets Qt isolés (Layer 2, sans automation OS)

# --- Plugins IA (optionnels, lourds, nécessitent GPU recommandé) ---
# Décommenter selon les besoins :
# torch>=2.2.0                # Base PyTorch (CUDA ou CPU)
# torchvision>=0.17.0
# basicsr>=1.4.2              # Real-ESRGAN, GFPGAN
# realesrgan>=0.3.0           # Super-résolution ×2/×4
# gfpgan>=1.3.8               # Amélioration visages
# iopaint>=1.3.3              # Réparation (inpainting)
