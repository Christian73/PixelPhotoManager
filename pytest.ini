[pytest]
markers =
    e2e: end-to-end scenario driving the real application through pywinauto (slow, Windows-only, steals the focus)
    gui: Qt widget test through pytest-qt (fast, no OS automation)
addopts = -m "not e2e"
testpaths = tests
