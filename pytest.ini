[pytest]
markers =
    e2e: scenario bout-en-bout pilotant la vraie application via pywinauto (lent, Windows-only, vole le focus)
    gui: test de widget Qt via pytest-qt (rapide, sans automation OS)
addopts = -m "not e2e"
testpaths = tests
